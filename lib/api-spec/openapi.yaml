openapi: 3.1.0
info:
  # Do not change the title, if the title changes, the import paths will be broken
  title: Api
  version: 0.3.0
  description: API specification for Assistente de Vistoria Condominial
servers:
  - url: /api
    description: Base API path
tags:
  - name: health
    description: Health operations
  - name: report
    description: Condominium inspection report operations
  - name: users
    description: User management
  - name: inspections
    description: Inspection history
  - name: condominios
    description: Condominium management
  - name: areas
    description: Inspection area management
  - name: assets
    description: Asset (CMDB) management
  - name: dashboard
    description: Dashboard summary
  - name: contratos
    description: Contract management per condominium
  - name: prestadores
    description: Service provider management
  - name: documentos
    description: Document and inspection report management
  - name: seguro
    description: Building insurance management
paths:
  /healthz:
    get:
      operationId: healthCheck
      tags: [health]
      summary: Health check
      responses:
        "200":
          description: Healthy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthStatus"

  /generate-report:
    post:
      operationId: generateReport
      tags: [report]
      summary: Generate inspection report
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                audio:
                  type: string
                  format: binary
                images:
                  type: array
                  items:
                    type: string
                    format: binary
                notes:
                  type: string
              required:
                - audio
      responses:
        "200":
          description: Report generated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InspectionReport"
        "400":
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: AI processing error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /inspections:
    get:
      operationId: listInspections
      tags: [inspections]
      summary: List inspections
      parameters:
        - name: page
          in: query
          schema:
            type: integer
            default: 1
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
        - name: urgencia
          in: query
          schema:
            type: string
            enum: [baixa, média, alta]
        - name: status
          in: query
          schema:
            type: string
            enum: [rascunho, gerado, pronto_para_envio, enviado]
        - name: condominioId
          in: query
          schema:
            type: integer
        - name: areaId
          in: query
          schema:
            type: integer
      responses:
        "200":
          description: List of inspections
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InspectionListResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    post:
      operationId: saveInspection
      tags: [inspections]
      summary: Save an inspection report to history
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SaveInspectionBody"
      responses:
        "201":
          description: Inspection saved
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SavedInspection"
        "400":
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /inspections/{id}:
    get:
      operationId: getInspection
      tags: [inspections]
      summary: Get a single inspection
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Inspection details
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SavedInspection"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /inspections/{id}/status:
    patch:
      operationId: updateInspectionStatus
      tags: [inspections]
      summary: Update inspection status (sindico only)
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateInspectionStatusBody"
      responses:
        "200":
          description: Status updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SavedInspection"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /users/me:
    get:
      operationId: getMe
      tags: [users]
      summary: Get current user profile
      responses:
        "200":
          description: Current user
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserProfile"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /users:
    get:
      operationId: listUsers
      tags: [users]
      summary: List all users (admin only)
      responses:
        "200":
          description: List of users
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/UserProfile"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /users/{clerkId}/role:
    patch:
      operationId: updateUserRole
      tags: [users]
      summary: Update user role (admin only)
      parameters:
        - name: clerkId
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateUserRoleBody"
      responses:
        "200":
          description: Role updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserProfile"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /users/{clerkId}/condominios:
    get:
      operationId: getUserCondominios
      tags: [users]
      summary: Get condominios associated with a user
      parameters:
        - name: clerkId
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: User condominios
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Condominio"
    post:
      operationId: addUserCondominio
      tags: [users]
      summary: Associate a user with a condominio (admin only)
      parameters:
        - name: clerkId
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UserCondominioBody"
      responses:
        "200":
          description: Association created
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                required:
                  - ok

  /users/{clerkId}/condominios/{condominioId}:
    delete:
      operationId: removeUserCondominio
      tags: [users]
      summary: Remove user from a condominio (admin only)
      parameters:
        - name: clerkId
          in: path
          required: true
          schema:
            type: string
        - name: condominioId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Association removed
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                required:
                  - ok

  /condominios:
    get:
      operationId: listCondominios
      tags: [condominios]
      summary: List condominios accessible to the current user
      responses:
        "200":
          description: List of condominios
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Condominio"
    post:
      operationId: createCondominio
      tags: [condominios]
      summary: Create a condominio (admin only)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CondominioBody"
      responses:
        "201":
          description: Condominio created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Condominio"

  /condominios/{id}:
    get:
      operationId: getCondominio
      tags: [condominios]
      summary: Get a single condominio
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Condominio details
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Condominio"
    patch:
      operationId: updateCondominio
      tags: [condominios]
      summary: Update a condominio (admin only)
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CondominioBody"
      responses:
        "200":
          description: Condominio updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Condominio"

  /condominios/{condominioId}/areas:
    get:
      operationId: listAreas
      tags: [areas]
      summary: List areas for a condominio
      parameters:
        - name: condominioId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: List of areas
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Area"
    post:
      operationId: createArea
      tags: [areas]
      summary: Create area (admin or sindico)
      parameters:
        - name: condominioId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AreaBody"
      responses:
        "201":
          description: Area created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Area"

  /condominios/{condominioId}/areas/{areaId}:
    patch:
      operationId: updateArea
      tags: [areas]
      summary: Update an area (admin or sindico)
      parameters:
        - name: condominioId
          in: path
          required: true
          schema:
            type: integer
        - name: areaId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AreaBody"
      responses:
        "200":
          description: Area updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Area"
    delete:
      operationId: deleteArea
      tags: [areas]
      summary: Delete an area (admin or sindico)
      parameters:
        - name: condominioId
          in: path
          required: true
          schema:
            type: integer
        - name: areaId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Area deleted
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                required:
                  - ok

  /public/assets/{assetId}:
    get:
      operationId: getPublicAsset
      tags: [assets]
      summary: Get public asset info (for QR code scans, no auth)
      parameters:
        - name: assetId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Public asset details
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PublicAsset"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /condominios/{condominioId}/assets:
    get:
      operationId: listAssets
      tags: [assets]
      summary: List assets for a condominio
      parameters:
        - name: condominioId
          in: path
          required: true
          schema:
            type: integer
        - name: areaId
          in: query
          schema:
            type: integer
        - name: tipo
          in: query
          schema:
            type: string
            enum: [equipamento, estrutura, sistema]
        - name: criticidade
          in: query
          schema:
            type: string
            enum: [baixa, media, alta]
        - name: status
          in: query
          schema:
            type: string
            enum: [operacional, em_manutencao, inativo]
      responses:
        "200":
          description: List of assets
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Asset"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    post:
      operationId: createAsset
      tags: [assets]
      summary: Create an asset (admin or sindico)
      parameters:
        - name: condominioId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetBody"
      responses:
        "201":
          description: Asset created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Asset"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /condominios/{condominioId}/assets/{assetId}:
    get:
      operationId: getAsset
      tags: [assets]
      summary: Get a single asset
      parameters:
        - name: condominioId
          in: path
          required: true
          schema:
            type: integer
        - name: assetId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Asset details
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Asset"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    patch:
      operationId: updateAsset
      tags: [assets]
      summary: Update an asset (admin or sindico)
      parameters:
        - name: condominioId
          in: path
          required: true
          schema:
            type: integer
        - name: assetId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetBody"
      responses:
        "200":
          description: Asset updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Asset"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    delete:
      operationId: deleteAsset
      tags: [assets]
      summary: Delete an asset (admin or sindico)
      parameters:
        - name: condominioId
          in: path
          required: true
          schema:
            type: integer
        - name: assetId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Asset deleted
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                required:
                  - ok

  /condominios/{condominioId}/contratos:
    get:
      operationId: listContratos
      tags: [contratos]
      summary: List contracts for a condominium
      parameters:
        - name: condominioId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: List of contracts
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ContratoCondominio"
    post:
      operationId: createContrato
      tags: [contratos]
      summary: Create a contract
      parameters:
        - name: condominioId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ContratoCondominioBody"
      responses:
        "201":
          description: Contract created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ContratoCondominio"

  /condominios/{condominioId}/contratos/{contratoId}:
    patch:
      operationId: updateContrato
      tags: [contratos]
      summary: Update a contract
      parameters:
        - name: condominioId
          in: path
          required: true
          schema:
            type: integer
        - name: contratoId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ContratoCondominioBody"
      responses:
        "200":
          description: Contract updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ContratoCondominio"
    delete:
      operationId: deleteContrato
      tags: [contratos]
      summary: Delete a contract
      parameters:
        - name: condominioId
          in: path
          required: true
          schema:
            type: integer
        - name: contratoId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Contract deleted
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                required:
                  - ok

  /condominios/{condominioId}/prestadores:
    get:
      operationId: listPrestadores
      tags: [prestadores]
      summary: List service providers for a condominium
      parameters:
        - name: condominioId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: List of service providers
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/PrestadorCondominio"
    post:
      operationId: createPrestador
      tags: [prestadores]
      summary: Create a service provider
      parameters:
        - name: condominioId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PrestadorCondominioBody"
      responses:
        "201":
          description: Provider created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PrestadorCondominio"

  /condominios/{condominioId}/prestadores/{prestadorId}:
    patch:
      operationId: updatePrestador
      tags: [prestadores]
      summary: Update a service provider
      parameters:
        - name: condominioId
          in: path
          required: true
          schema:
            type: integer
        - name: prestadorId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PrestadorCondominioBody"
      responses:
        "200":
          description: Provider updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PrestadorCondominio"
    delete:
      operationId: deletePrestador
      tags: [prestadores]
      summary: Delete a service provider
      parameters:
        - name: condominioId
          in: path
          required: true
          schema:
            type: integer
        - name: prestadorId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Provider deleted
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                required:
                  - ok

  /condominios/{condominioId}/documentos:
    get:
      operationId: listDocumentos
      tags: [documentos]
      summary: List documents for a condominium
      parameters:
        - name: condominioId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: List of documents
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/DocumentoCondominio"
    post:
      operationId: createDocumento
      tags: [documentos]
      summary: Create a document record
      parameters:
        - name: condominioId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DocumentoCondominioBody"
      responses:
        "201":
          description: Document created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DocumentoCondominio"

  /condominios/{condominioId}/documentos/{documentoId}:
    patch:
      operationId: updateDocumento
      tags: [documentos]
      summary: Update a document record
      parameters:
        - name: condominioId
          in: path
          required: true
          schema:
            type: integer
        - name: documentoId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DocumentoCondominioBody"
      responses:
        "200":
          description: Document updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DocumentoCondominio"
    delete:
      operationId: deleteDocumento
      tags: [documentos]
      summary: Delete a document record
      parameters:
        - name: condominioId
          in: path
          required: true
          schema:
            type: integer
        - name: documentoId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Document deleted
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                required:
                  - ok

  /condominios/{condominioId}/seguro:
    get:
      operationId: getSeguroPredial
      tags: [seguro]
      summary: Get building insurance for a condominium (null if none)
      parameters:
        - name: condominioId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Insurance record or null
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SeguroPredial"
        "404":
          description: No insurance record
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    put:
      operationId: upsertSeguroPredial
      tags: [seguro]
      summary: Create or update building insurance
      parameters:
        - name: condominioId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SeguroPredialBody"
      responses:
        "200":
          description: Insurance saved
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SeguroPredial"

  /condominios/{condominioId}/health:
    get:
      operationId: getCondominioHealth
      tags: [condominios]
      summary: Get health/alert summary for a condominium
      parameters:
        - name: condominioId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Health summary with expiry counts
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CondominioHealth"

  /dashboard/summary:
    get:
      operationId: getDashboardSummary
      tags: [dashboard]
      summary: Get dashboard summary for current user
      responses:
        "200":
          description: Dashboard summary
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DashboardSummary"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /assembleias/insights:
    get:
      operationId: getAssembleiasInsights
      summary: Get assembly insights based on inspections and improvements
      tags: [Assembleias]
      parameters:
        - in: query
          name: condominioId
          schema:
            type: integer
        - in: query
          name: months
          schema:
            type: integer
            default: 6
      responses:
        "200":
          description: Assembly insights
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssembleiasInsights"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

components:
  schemas:
    HealthStatus:
      type: object
      properties:
        status:
          type: string
      required:
        - status

    InspectionReport:
      type: object
      properties:
        tipo:
          type: string
        urgencia:
          type: string
          enum: [baixa, média, alta]
        acao:
          type: string
        resumo:
          type: string
        comunicado:
          type: string
        transcricao:
          type: string
        analise_imagens:
          type: string
      required:
        - tipo
        - urgencia
        - acao
        - resumo
        - comunicado

    SaveInspectionBody:
      type: object
      properties:
        tipo:
          type: string
        urgencia:
          type: string
          enum: [baixa, média, alta]
        acao:
          type: string
        resumo:
          type: string
        comunicado:
          type: string
        transcricao:
          type: string
        analise_imagens:
          type: string
        local:
          type: string
        condominio:
          type: string
        condominioId:
          type: integer
        areaId:
          type: integer
        assetId:
          type: integer
        tipoEvento:
          type: string
          enum: [vistoria, manutencao, incidente, melhoria]
        tipoVistoria:
          type: string
          enum: [tecnica, predial, checklist, acompanhamento_diario, acompanhamento_semanal, entrega_chaves, emergencial]
        escopo:
          type: string
          enum: [completa, areas_especificas]
        areasIds:
          type: string
      required:
        - tipo
        - urgencia
        - acao
        - resumo
        - comunicado

    UpdateInspectionStatusBody:
      type: object
      properties:
        status:
          type: string
          enum: [pronto_para_envio, enviado]
      required:
        - status

    SavedInspection:
      type: object
      properties:
        id:
          type: integer
        tipo:
          type: string
        urgencia:
          type: string
          enum: [baixa, média, alta]
        acao:
          type: string
        resumo:
          type: string
        comunicado:
          type: string
        transcricao:
          type: string
        analise_imagens:
          type: string
        local:
          type: string
        condominio:
          type: string
        status:
          type: string
          enum: [rascunho, gerado, pronto_para_envio, enviado]
        condominioId:
          type: integer
        areaId:
          type: integer
        assetId:
          type: integer
        tipoEvento:
          type: string
          enum: [vistoria, manutencao, incidente, melhoria]
        tipoVistoria:
          type: string
          nullable: true
          enum: [tecnica, predial, checklist, acompanhamento_diario, acompanhamento_semanal, entrega_chaves, emergencial]
        escopo:
          type: string
          enum: [completa, areas_especificas]
        areasIds:
          type: string
          nullable: true
        createdByClerkId:
          type: string
        createdAt:
          type: string
          format: date-time
      required:
        - id
        - tipo
        - urgencia
        - acao
        - resumo
        - comunicado
        - status
        - tipoEvento
        - escopo
        - createdByClerkId
        - createdAt

    InspectionListResponse:
      type: object
      properties:
        inspections:
          type: array
          items:
            $ref: "#/components/schemas/SavedInspection"
        total:
          type: integer
        page:
          type: integer
        limit:
          type: integer
      required:
        - inspections
        - total
        - page
        - limit

    UserProfile:
      type: object
      properties:
        id:
          type: integer
        clerkId:
          type: string
        email:
          type: string
        name:
          type: string
        role:
          type: string
          enum: [admin, sindico, vistoriador]
        condominio:
          type: string
        condominioIds:
          type: array
          items:
            type: integer
        createdAt:
          type: string
          format: date-time
      required:
        - id
        - clerkId
        - email
        - role
        - createdAt

    UpdateUserRoleBody:
      type: object
      properties:
        role:
          type: string
          enum: [admin, sindico, vistoriador]
        condominio:
          type: string
      required:
        - role

    UserCondominioBody:
      type: object
      properties:
        condominioId:
          type: integer
      required:
        - condominioId

    Condominio:
      type: object
      properties:
        id:
          type: integer
        nome:
          type: string
        cnpj:
          type: string
          nullable: true
        tipoCondominio:
          type: string
          enum: [residencial, comercial, misto]
        endereco:
          type: string
          nullable: true
        cep:
          type: string
          nullable: true
        bairro:
          type: string
          nullable: true
        cidade:
          type: string
          nullable: true
        estado:
          type: string
          nullable: true
        totalUnidades:
          type: integer
          nullable: true
        totalBlocos:
          type: integer
          nullable: true
        totalAndares:
          type: integer
          nullable: true
        anoConstrucao:
          type: integer
          nullable: true
        telefone:
          type: string
          nullable: true
        email:
          type: string
          nullable: true
        sindico:
          type: string
          nullable: true
        zelador:
          type: string
          nullable: true
        administradora:
          type: string
          nullable: true
        inscricaoMunicipal:
          type: string
          nullable: true
        areaTotalM2:
          type: number
          nullable: true
        areaLazerM2:
          type: number
          nullable: true
        numElevadores:
          type: integer
          nullable: true
        tipoPortaria:
          type: string
          nullable: true
        equipe:
          $ref: "#/components/schemas/EquipeData"
          nullable: true
        ativo:
          type: boolean
        createdAt:
          type: string
          format: date-time
      required:
        - id
        - nome
        - tipoCondominio
        - ativo
        - createdAt

    CondominioBody:
      type: object
      properties:
        nome:
          type: string
        cnpj:
          type: string
        tipoCondominio:
          type: string
          enum: [residencial, comercial, misto]
        endereco:
          type: string
        cep:
          type: string
        bairro:
          type: string
        cidade:
          type: string
        estado:
          type: string
        totalUnidades:
          type: integer
        totalBlocos:
          type: integer
        totalAndares:
          type: integer
        anoConstrucao:
          type: integer
        telefone:
          type: string
        email:
          type: string
        sindico:
          type: string
        zelador:
          type: string
        administradora:
          type: string
        inscricaoMunicipal:
          type: string
        areaTotalM2:
          type: number
        areaLazerM2:
          type: number
        numElevadores:
          type: integer
        tipoPortaria:
          type: string
        equipe:
          $ref: "#/components/schemas/EquipeData"
        ativo:
          type: boolean
      required:
        - nome

    Area:
      type: object
      properties:
        id:
          type: integer
        condominioId:
          type: integer
        nome:
          type: string
        tipo:
          type: string
        bloco:
          type: string
          nullable: true
        andar:
          type: integer
          nullable: true
        privacidade:
          type: string
          enum: [publica, privada, mista]
        descricao:
          type: string
          nullable: true
        capacidade:
          type: integer
          nullable: true
        reservavel:
          type: boolean
        horarioAbertura:
          type: string
          nullable: true
        horarioFechamento:
          type: string
          nullable: true
        ativo:
          type: boolean
        createdAt:
          type: string
          format: date-time
      required:
        - id
        - condominioId
        - nome
        - tipo
        - privacidade
        - reservavel
        - ativo
        - createdAt

    AreaBody:
      type: object
      properties:
        nome:
          type: string
        tipo:
          type: string
        bloco:
          type: string
        andar:
          type: integer
        privacidade:
          type: string
          enum: [publica, privada, mista]
        descricao:
          type: string
        capacidade:
          type: integer
        reservavel:
          type: boolean
        horarioAbertura:
          type: string
        horarioFechamento:
          type: string
      required:
        - nome
        - tipo

    Asset:
      type: object
      properties:
        id:
          type: integer
        condominioId:
          type: integer
        areaId:
          type: integer
        nome:
          type: string
        tipo:
          type: string
          enum: [equipamento, estrutura, sistema]
        criticidade:
          type: string
          enum: [baixa, media, alta]
        status:
          type: string
          enum: [operacional, em_manutencao, inativo]
        descricao:
          type: string
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
      required:
        - id
        - condominioId
        - nome
        - tipo
        - criticidade
        - status
        - createdAt
        - updatedAt

    PublicAsset:
      type: object
      properties:
        id:
          type: integer
        nome:
          type: string
        tipo:
          type: string
        criticidade:
          type: string
        status:
          type: string
        descricao:
          type: string
        condominioNome:
          type: string
        areaNome:
          type: string
        ultimasVistorias:
          type: array
          items:
            type: object
            properties:
              id:
                type: integer
              tipo:
                type: string
              urgencia:
                type: string
              resumo:
                type: string
              createdAt:
                type: string
                format: date-time
            required: [id, tipo, urgencia, resumo, createdAt]
      required:
        - id
        - nome
        - tipo
        - criticidade
        - status
        - condominioNome
        - ultimasVistorias

    AssetBody:
      type: object
      properties:
        areaId:
          type: integer
        nome:
          type: string
        tipo:
          type: string
          enum: [equipamento, estrutura, sistema]
        criticidade:
          type: string
          enum: [baixa, media, alta]
        status:
          type: string
          enum: [operacional, em_manutencao, inativo]
        descricao:
          type: string
      required:
        - nome
        - tipo
        - criticidade
        - status

    DashboardSummary:
      type: object
      properties:
        totalCondominios:
          type: integer
        totalAssets:
          type: integer
        assetsOperacional:
          type: integer
        assetsEmManutencao:
          type: integer
        assetsInativo:
          type: integer
        assetsCriticos:
          type: integer
        totalEventos30d:
          type: integer
        eventosAlta:
          type: integer
        eventosMedios:
          type: integer
        recentEvents:
          type: array
          items:
            $ref: "#/components/schemas/SavedInspection"
        criticalAssets:
          type: array
          items:
            $ref: "#/components/schemas/Asset"
      required:
        - totalCondominios
        - totalAssets
        - assetsOperacional
        - assetsEmManutencao
        - assetsInativo
        - assetsCriticos
        - totalEventos30d
        - eventosAlta
        - eventosMedios
        - recentEvents
        - criticalAssets

    AssembleiaTopic:
      type: object
      properties:
        titulo:
          type: string
        categoria:
          type: string
        prioridade:
          type: string
        descricao:
          type: string
        evidencias:
          type: integer
        condominios:
          type: array
          items:
            type: string
        tiposEvento:
          type: array
          items:
            type: string
        urgenciaMedia:
          type: string
      required:
        - titulo
        - categoria
        - prioridade
        - descricao
        - evidencias
        - condominios
        - tiposEvento
        - urgenciaMedia

    AssembleiasInsights:
      type: object
      properties:
        topics:
          type: array
          items:
            $ref: "#/components/schemas/AssembleiaTopic"
        totalInspections:
          type: integer
        periodMonths:
          type: integer
        generatedAt:
          type: string
          format: date-time
      required:
        - topics
        - totalInspections
        - periodMonths
        - generatedAt

    EquipeData:
      type: object
      properties:
        subSindico:
          type: object
          nullable: true
          properties:
            nome:
              type: string
            telefone:
              type: string
            email:
              type: string
        zelador:
          type: object
          nullable: true
          properties:
            tipo:
              type: string
            nome:
              type: string
            telefone:
              type: string
            empresaTerceirizadora:
              type: string
        portaria:
          type: object
          nullable: true
          properties:
            tipo:
              type: string
            empresa:
              type: string
        asg:
          type: object
          nullable: true
          properties:
            quantidade:
              type: integer
            empresa:
              type: string
        seguranca:
          type: object
          nullable: true
          properties:
            quantidade:
              type: integer
            empresa:
              type: string

    ContratoCondominio:
      type: object
      properties:
        id:
          type: integer
        condominioId:
          type: integer
        tipoServico:
          type: string
        empresa:
          type: string
          nullable: true
        cnpjEmpresa:
          type: string
          nullable: true
        telefoneEmpresa:
          type: string
          nullable: true
        emailEmpresa:
          type: string
          nullable: true
        valorMensal:
          type: number
          nullable: true
        vigenciaInicio:
          type: string
          nullable: true
        vigenciaFim:
          type: string
          nullable: true
        periodicidadeVisita:
          type: string
          nullable: true
        statusVigencia:
          type: string
          enum: [ativo, a_vencer, vencido]
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
      required:
        - id
        - condominioId
        - tipoServico
        - statusVigencia
        - createdAt
        - updatedAt

    ContratoCondominioBody:
      type: object
      properties:
        tipoServico:
          type: string
        empresa:
          type: string
        cnpjEmpresa:
          type: string
        telefoneEmpresa:
          type: string
        emailEmpresa:
          type: string
        valorMensal:
          type: number
        vigenciaInicio:
          type: string
        vigenciaFim:
          type: string
        periodicidadeVisita:
          type: string
      required:
        - tipoServico

    PrestadorCondominio:
      type: object
      properties:
        id:
          type: integer
        condominioId:
          type: integer
        nome:
          type: string
        especialidade:
          type: string
          nullable: true
        telefone:
          type: string
          nullable: true
        email:
          type: string
          nullable: true
        avaliacao:
          type: integer
          nullable: true
        observacoes:
          type: string
          nullable: true
        createdAt:
          type: string
          format: date-time
      required:
        - id
        - condominioId
        - nome
        - createdAt

    PrestadorCondominioBody:
      type: object
      properties:
        nome:
          type: string
        especialidade:
          type: string
        telefone:
          type: string
        email:
          type: string
        avaliacao:
          type: integer
        observacoes:
          type: string
      required:
        - nome

    DocumentoCondominio:
      type: object
      properties:
        id:
          type: integer
        condominioId:
          type: integer
        tipo:
          type: string
        numeroReferencia:
          type: string
          nullable: true
        dataEmissao:
          type: string
          nullable: true
        dataValidade:
          type: string
          nullable: true
        empresaResponsavel:
          type: string
          nullable: true
        statusDocumento:
          type: string
          enum: [valido, a_vencer, vencido]
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
      required:
        - id
        - condominioId
        - tipo
        - statusDocumento
        - createdAt
        - updatedAt

    DocumentoCondominioBody:
      type: object
      properties:
        tipo:
          type: string
        numeroReferencia:
          type: string
        dataEmissao:
          type: string
        dataValidade:
          type: string
        empresaResponsavel:
          type: string
      required:
        - tipo

    SeguroPredial:
      type: object
      properties:
        id:
          type: integer
        condominioId:
          type: integer
        seguradora:
          type: string
          nullable: true
        numeroApolice:
          type: string
          nullable: true
        vigenciaInicio:
          type: string
          nullable: true
        vigenciaFim:
          type: string
          nullable: true
        tipoCobertura:
          type: string
          nullable: true
        valorSegurado:
          type: number
          nullable: true
        nomeCorretor:
          type: string
          nullable: true
        telefoneCorretor:
          type: string
          nullable: true
        statusVigencia:
          type: string
          enum: [ativo, a_vencer, vencido, sem_registro]
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
      required:
        - id
        - condominioId
        - statusVigencia
        - createdAt
        - updatedAt

    SeguroPredialBody:
      type: object
      properties:
        seguradora:
          type: string
        numeroApolice:
          type: string
        vigenciaInicio:
          type: string
        vigenciaFim:
          type: string
        tipoCobertura:
          type: string
        valorSegurado:
          type: number
        nomeCorretor:
          type: string
        telefoneCorretor:
          type: string

    CondominioHealth:
      type: object
      properties:
        condominioId:
          type: integer
        contratosVencidos:
          type: integer
        contratosAVencer:
          type: integer
        documentosVencidos:
          type: integer
        documentosAVencer:
          type: integer
        seguroVencido:
          type: boolean
        seguroAVencer:
          type: boolean
      required:
        - condominioId
        - contratosVencidos
        - contratosAVencer
        - documentosVencidos
        - documentosAVencer
        - seguroVencido
        - seguroAVencer

    ErrorResponse:
      type: object
      properties:
        error:
          type: string
        details:
          type: string
      required:
        - error
